{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Group TUnit family packages so coupled versions bump together. Split PRs leave one package behind and the other ahead, causing NU1605 (downgrade) in CI. groupSlug pins the branch to renovate/tunit so a stale per-package branch cannot prevent regrouping.",
      "groupName": "tunit",
      "groupSlug": "tunit",
      "matchManagers": [
        "nuget"
      ],
      "matchPackageNames": [
        "TUnit",
        "TUnit.Engine",
        "TUnit.Core",
        "TUnit.Assertions",
        "TUnit.Playwright",
        "TUnit.AspNetCore"
      ]
    }
  ]
}
