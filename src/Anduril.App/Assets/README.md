# Anduril Desktop Assets

- SVG assets in this folder should be monochrome unless a light/dark variant is explicitly required.
- Assets intended for runtime recoloring should use `fill="currentColor"` and `stroke="currentColor"`.
- Provider marks live under `providers/` and should follow the same `currentColor` convention.
